# .env.test — used by Vitest and Playwright CI runs
# Points to a staging/mock API — never the production API

VITE_API_URL=http://localhost:8000/api
VITE_GOOGLE_MAPS_API_KEY=fake-maps-key-for-tests

# Playwright E2E credentials (real test user on staging)
E2E_TEST_EMAIL=e2e@rentusco.com
E2E_TEST_PASSWORD=e2eTestPassword123!
